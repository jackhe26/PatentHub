# BabelDOC Python 依赖包
# 适用版本：babeldoc 0.5.24
# Python 版本要求：3.10 ~ 3.12（推荐 3.11）
#
# 安装方式（推荐使用国内镜像）：
#   pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
#
# 详细安装指南见：docs/babeldoc-install-guide.md

# ── AI 翻译 ──────────────────────────────────────────────
openai>=1.30.0
httpx>=0.27.0
tenacity>=8.3.0

# ── PDF 处理 ─────────────────────────────────────────────
PyMuPDF>=1.24.0
freetype-py>=2.4.0
bitstring>=4.2.0

# ── 文档布局分析（ONNX 模型）────────────────────────────
numpy>=1.26.0
# Windows / Linux：
onnx>=1.16.0
onnxruntime>=1.18.0
# macOS Apple Silicon 请改用：
#   onnxruntime-silicon>=1.16.0
# 有 NVIDIA GPU 可改用：
#   onnxruntime-gpu>=1.18.0

# ── 计算机视觉 ───────────────────────────────────────────
# 无 GUI 服务器环境推荐 headless 版：
opencv-python-headless>=4.9.0
# 桌面环境可用完整版（二选一）：
# opencv-python>=4.9.0

# ── 翻译缓存（SQLite ORM）────────────────────────────────
peewee>=3.17.0

# ── Token 计数 ───────────────────────────────────────────
tiktoken>=0.7.0

# ── 数据序列化 ───────────────────────────────────────────
orjson>=3.10.0
msgpack>=1.0.8
xsdata>=24.4

# ── 术语表 / 正则 ────────────────────────────────────────
regex>=2024.4.0
chardet>=5.2.0
# hyperscan：高性能正则匹配，Windows 安装见 install guide
hyperscan>=0.7.7

# ── 终端输出 / 进度条 ────────────────────────────────────
rich>=13.7.0
tqdm>=4.66.0

# ── 命令行参数 ───────────────────────────────────────────
configargparse>=1.7.0

# ── 可选：表格 OCR（仅 --translate-table-text 时需要）────
# rapidocr_onnxruntime>=1.3.0
