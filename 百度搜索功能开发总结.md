# 百度搜索功能开发总结

## 开发时间
2026年5月30日

---

## 一、新增功能：百度搜索

### 1. 功能说明
- 新增百度搜索作为网络搜索提供方
- 专门针对中文查询优化，适合国内用户使用
- 免费使用，无需API Key

### 2. 修改的文件清单

| 文件路径 | 操作 | 说明 |
|---------|------|------|
| `src/renderer/packages/web-search/baidu.ts` | 新建 | 百度搜索实现类 |
| `src/shared/types/settings.ts` | 修改 | 在枚举中添加 'baidu' 选项 |
| `src/renderer/packages/web-search/index.ts` | 修改 | 导入并注册百度搜索 |
| `src/renderer/routes/settings/web-search.tsx` | 修改 | 在设置界面添加百度搜索选项 |

---

## 二、优化改进

### 1. 移除收费选项
- 从设置界面下拉菜单中移除了 "Chatbox Search (Pro)" 收费选项
- 用户不再需要付费即可使用网络搜索功能

### 2. 默认搜索提供方调整
- 将默认搜索从 'build-in' (Chatbox Search) 改为 'baidu' (百度搜索)
- 修改文件：`src/shared/defaults.ts`

---

## 三、清理操作

### 1. 用户数据备份
- 备份位置：`C:\Users\jackhe2023\PatentHub_Backup_2026_05_30`
- 备份内容：包含所有配置文件、缓存、会话数据等

### 2. 清除缓存
- 清除了 `%APPDATA%\PatentHub` 的所有缓存数据
- 项目启动后如同全新安装

---

## 四、技术实现要点

### 1. 搜索提供方实现模式
- 参考现有 Bing、Tavily 实现
- 继承 WebSearch 基类
- 实现 search() 方法

### 2. DOM解析
- 使用浏览器原生 DOMParser 解析百度搜索结果页面
- 无需额外依赖库

### 3. CSS选择器适配
百度搜索结果页面使用的选择器：
- `.result` - 通用结果容器
- `.c-container` - 百度特有容器
- `h3>a` - 标题链接
- `.c-abstract` - 摘要内容

---

## 五、代码示例

### 百度搜索类核心代码
```typescript
export class BaiduSearch extends WebSearch {
  async search(query: string, signal?: AbortSignal): Promise<SearchResult> {
    const html = await this.fetchSerp(query, signal)
    const items = this.extractItems(html)
    return { items }
  }

  private async fetchSerp(query: string, signal?: AbortSignal) {
    const html = await this.fetch('https://www.baidu.com/s', {
      method: 'GET',
      query: { wd: query },
      signal,
    })
    return html as string
  }

  private extractItems(html: string) {
    const dom = new DOMParser().parseFromString(html, 'text/html')
    const nodes = dom.querySelectorAll('.result, .c-container')
    return Array.from(nodes)
      .slice(0, 10)
      .map((node) => {
        const nodeA = node.querySelector('h3>a, .t>a, a[href*="http"]')
        const link = nodeA?.getAttribute('href') || ''
        const title = nodeA?.textContent || ''
        const nodeAbstract = node.querySelector('.c-abstract, .c-span-text')
        const snippet = nodeAbstract?.textContent || ''
        return { title, link, snippet }
      })
      .filter(item => item.link && item.link.includes('http'))
  }
}
```

---

## 六、经验总结

1. **复用现有架构**：新搜索提供方的实现模式与现有 Bing、Tavily 一致，降低了开发成本
2. **无需后端支持**：通过爬取百度搜索结果页面获取数据，无需额外 API
3. **适合国内用户**：百度搜索对中文查询支持更好，访问速度快
4. **数据安全**：用户数据已备份，清除缓存后如需恢复可从备份目录复制

---

## 七、后续优化建议

1. **增加更多搜索引擎**：可考虑添加搜狗、360搜索等
2. **搜索结果缓存优化**：根据用户习惯优化缓存策略
3. **结果质量改进**：优化选择器以适应百度页面变化

---

*文档创建时间：2026年5月30日*
